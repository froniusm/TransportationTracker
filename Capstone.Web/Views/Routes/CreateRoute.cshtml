@using Capstone.Web.Models.ViewModel;
@model UserRoutesView

@{
    ViewBag.Title = "CreateRoute";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Routes</h2>

@*@using (Html.BeginForm("CreateRoute", "Routes", FormMethod.Post))
{
    <table class="table-responsive table-striped table-bordered">
        @foreach (var route in Model.AllRoutes)
        {
            <tr>
                @route.Key.Name
            </tr>
        }
        <tr>
            @Html.TextBoxFor(m => m.AllRoutes., new { @class = "form-control", placeholder = "Add a route" })
        </tr>
    </table>
    <button type="submit" class="btn btn-primary">Submit</button>*@
@*}*@